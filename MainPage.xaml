<Page
    x:Class="Photo_Library_App.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Photo_Library_App"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <GridView x:Name="Images" ItemsSource="{Binding}" Height=" 300" Width="400" Margin="50">
            <GridView.ItemTemplate>
                <DataTemplate x:DataType="Image">
                    <StackPanel Orientation="Vertical" Margin="2">
                        <Image Source="File"/>
                    </StackPanel>
                </DataTemplate>


            </GridView.ItemTemplate>
        </GridView>
        <Button x:Name="Import_Photos_Button" Content="Import Photos" Margin=" 100" Click="Import_Photos_Button_ClickAsync"/>
       
    </Grid>
</Page>
